define("BlipModel", function () {
    var BlipModel = Backbone.Model.extend({
        latitude : undefined,
        longitude : undefined,

        initialize : function () {

        }

    });

    return BlipModel;
});